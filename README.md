# spotifyRecAlgoImproved
I use Spotify API to get a user's top tracks and understand their taste preferences. I use this to weight different features in a song (energy, danceability etc.). I let the user input a song and then perform k-means clustering on a dataset of songs to determine what other songs the user would like.
